name: CI CD Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
      - run: echo " This is First stepp"
      - name: :Print God bye
      - run:  echo "Good bye.....workflow completed"
    
